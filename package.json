{
  "name": "quillmusic-dev-runner",
  "version": "0.1.0",
  "private": true,
  "description": "QuillMusic unified development runner",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"cd quillmusic/backend && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000\" \"cd quillmusic/frontend && npm run dev -- --host 0.0.0.0\"",
    "install:all": "cd quillmusic/backend && pip install -r requirements.txt && cd ../frontend && npm install",
    "test:backend": "cd quillmusic/backend && pytest",
    "test:frontend": "cd quillmusic/frontend && npm run build",
    "build:frontend": "cd quillmusic/frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
