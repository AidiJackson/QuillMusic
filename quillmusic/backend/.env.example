# QuillMusic Backend Environment Variables

# App Configuration
APP_NAME=QuillMusic
APP_VERSION=0.1.0
DEBUG=true

# API Configuration
API_PREFIX=/api

# Redis Configuration
REDIS_URL=redis://redis:6379/0

# CORS Origins (comma-separated)
# For local development
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Audio Provider Configuration
# Options: "fake" (default, no real audio), "stable_audio_http" (external API)
QUILLMUSIC_AUDIO_PROVIDER=fake

# External Audio Provider Settings (only needed if AUDIO_PROVIDER=stable_audio_http)
# QUILLMUSIC_AUDIO_API_BASE_URL=https://api.your-audio-provider.com
# QUILLMUSIC_AUDIO_API_KEY=sk-your-api-key-here
